<!DOCTYPE html>
<html class="h-full">
  <head>
    <title><%= content_for(:title) || "MedDir - Your Medical Records Hub" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="apple-mobile-web-app-title" content="MedDir">
    <meta name="application-name" content="MedDir">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="theme-color" content="#10b981">
    <meta name="msapplication-TileColor" content="#0f172a">
    <meta name="msapplication-TileImage" content="/icons/icon-144x144.png">
    <meta name="description" content="Organize and share your medical documents with healthcare providers">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# PWA Manifest %>
    <link rel="manifest" href="/manifest.json">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">

    <link rel="icon" href="/icons/icon-96x96.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icons/icon-192x192.png">
    <link rel="apple-touch-icon" sizes="152x152" href="/icons/icon-152x152.png">
    <link rel="apple-touch-icon" sizes="180x180" href="/icons/icon-192x192.png">
    <link rel="apple-touch-icon" sizes="167x167" href="/icons/icon-192x192.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <style>
      body { font-family: 'Plus Jakarta Sans', system-ui, sans-serif; }
    </style>
  </head>

  <body class="h-full bg-slate-900 antialiased">
    <% if notice.present? && !content_for?(:hide_flash) %>
      <div class="fixed top-4 right-4 z-50 max-w-sm" data-controller="flash" data-flash-dismiss-after-value="5000">
        <div class="bg-emerald-500/10 border border-emerald-500/50 rounded-xl p-4 backdrop-blur-xl shadow-lg">
          <div class="flex items-center gap-3">
            <svg class="w-5 h-5 text-emerald-400 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            <span class="text-emerald-300 text-sm font-medium"><%= notice %></span>
          </div>
        </div>
      </div>
    <% end %>

    <% if alert.present? && !content_for?(:hide_flash) %>
      <div class="fixed top-4 right-4 z-50 max-w-sm" data-controller="flash" data-flash-dismiss-after-value="5000">
        <div class="bg-red-500/10 border border-red-500/50 rounded-xl p-4 backdrop-blur-xl shadow-lg">
          <div class="flex items-center gap-3">
            <svg class="w-5 h-5 text-red-400 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            <span class="text-red-300 text-sm font-medium"><%= alert %></span>
          </div>
        </div>
      </div>
    <% end %>

    <%= yield %>

    <%# PWA offline status indicator - only for authenticated users %>
    <% if defined?(current_user) && current_user %>
      <%= render "shared/offline_status" %>
    <% end %>
  </body>
</html>
